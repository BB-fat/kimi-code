---
"@moonshot-ai/kimi-code": minor
---

Allow the Agent and AgentSwarm tools to run each subagent on any configured model alias. Pass `model` with a `[models]` id, or the shortcuts `primary` / `secondary`.
